server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/user_db
    username: postgres
    password: local_dev_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true